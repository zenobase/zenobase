dependencies:
  post:
    - sudo pip install --upgrade awscli

test:
  override:
    - play dist

deployment:
  production:
    branch: master
    commands:
      - aws s3 cp target/universal/zenobase-SNAPSHOT.zip s3://zeno-builds/zenobase-$CIRCLE_BUILD_NUM.zip
