name: Build and Push Play

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [master]
    paths:
      - 'app/**'
      - 'conf/**'
      - 'docker/play/**'
      - 'project/**'
      - 'public/**'
      - 'build.sbt'
  workflow_call:
    inputs:
      image-tag:
        required: true
        type: string
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  SBT_OPTS: "-Xmx2g"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-sbt

      - name: Test
        run: sbt 'test-only *Test'

      - name: Build tarball
        run: sbt universal:packageZipTarball

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Play image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/play/Dockerfile
          platforms: linux/arm64
          push: true
          tags: |
            ${{ steps.ecr-login.outputs.registry }}/zenobase-play:${{ github.sha }}
            ${{ steps.ecr-login.outputs.registry }}/zenobase-play:${{ inputs.image-tag || 'latest' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
