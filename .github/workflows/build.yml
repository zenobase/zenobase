name: Build

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

on:
    push:
        branches: [main]
        paths:
            - "docker/**"
            - "src/**"
            - "!src/main/java/com/zenobase/scripts/**"
            - "pom.xml"
    workflow_dispatch:

env:
    AWS_REGION: us-east-1

jobs:
    build-and-push:
        runs-on: ubuntu-24.04-arm
        permissions:
            id-token: write
            contents: read
        services:
            opensearch:
                image: opensearchproject/opensearch:3.5.0
                ports:
                    - 9200:9200
                env:
                    discovery.type: single-node
                    plugins.security.disabled: "true"
                    DISABLE_INSTALL_DEMO_CONFIG: "true"
                options: >-
                    --health-cmd "curl -f http://localhost:9200/_cluster/health || exit 1"
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 10
                    --health-start-period 30s
        steps:
            - uses: actions/checkout@v7

            - uses: actions/setup-java@v5
              with:
                  distribution: temurin
                  java-version: 25
                  cache: maven

            - name: Check formatting
              run: ./mvnw -B spotless:check

            - name: Test
              run: ./mvnw -B verify

            - name: Upload coverage to Codecov
              uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
              with:
                  token: ${{ secrets.CODECOV_TOKEN }}
                  files: target/site/jacoco/jacoco.xml
                  flags: backend

            - name: Package
              run: ./mvnw -B package -DskipTests

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@254c19bd240aabef8777f48595e9d2d7b972184b # v6.2.1
              with:
                  role-to-assume: ${{ vars.AWS_ROLE_ARN }}
                  aws-region: ${{ env.AWS_REGION }}

            - name: Login to ECR
              id: ecr-login
              uses: aws-actions/amazon-ecr-login@b040164c4934333d597f3f9c67502ff28f814e9c # v2.1.6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0

            - name: Build and push image
              uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
              with:
                  context: .
                  file: docker/Dockerfile
                  platforms: linux/arm64
                  push: true
                  tags: |
                      ${{ steps.ecr-login.outputs.registry }}/zenobase-api:${{ github.sha }}
                      ${{ steps.ecr-login.outputs.registry }}/zenobase-api:latest
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Create Sentry release
              run: |
                  npx @sentry/cli releases new "${{ github.sha }}"
                  npx @sentry/cli releases set-commits "${{ github.sha }}" --auto
                  npx @sentry/cli releases finalize "${{ github.sha }}"
              env:
                  SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
                  SENTRY_ORG: zenobase
                  SENTRY_PROJECT: zenobase-api
