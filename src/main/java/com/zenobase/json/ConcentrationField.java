package com.zenobase.json;

import javax.measure.quantity.VolumetricDensity;

public class ConcentrationField extends DecimalMeasureField<VolumetricDensity> {

	public ConcentrationField(String name) {
		super(name);
	}
}
