/**
 * MCP tools — parameterized queries over Zenobase event data. Each tool wraps the existing
 * {@link com.zenobase.search.EventSearchBuilder} + facet pipeline so the MCP surface and the REST API stay in lock
 * step semantically.
 */
@NullMarked
package com.zenobase.mcp.tools;

import org.jspecify.annotations.NullMarked;
