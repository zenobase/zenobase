=== IAM ===

policy-elasticsearch
{
  "Statement": [
    {
      "Action": [
        "s3:*"
      ],
      "Effect": "Allow",
      "Resource": [
        "arn:aws:s3:::zeno-elasticsearch",
        "arn:aws:s3:::zeno-elasticsearch/*"
      ]
    }
  ],
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "EC2:Describe*",
      "Resource": "*"
    }
  ]
}


=== Key ===

openssl genrsa -out scalarium.key 2046
openssl req -new -x509 -sha1 -days 3750 -key scalarium.key -out scalarium.pem


=== SSL Certificate (via namecheap.com) ===

# generate certificate signing request (csr)
openssl req -new -newkey rsa:2048 -keyout server.key -out server.csr
# z3nowhat

# for aws-lb:
openssl rsa -in server.key -text


=== EC2 ===

# AMI: ami-b89842d1 (Ubuntu Server 12.04 LTS, 32 bit, instance store)
# Instance Type: m1.small
# Availability Zone: us-east-1c
# Security Group: zeno


=== Install Packages ===

sudo aptitude update && sudo aptitude upgrade
sudo aptitude install openjdk-6-jre-headless
sudo aptitude install unzip
sudo aptitude install ntp


=== File Handles ===

sudoedit /etc/security/limits.conf
--
elasticsearch  -  nofile   64000
elasticsearch  -  memlock  unlimited 
--

sudoedit /etc/pam.d/su
--
session    required   pam_limits.so
--

# If elasticsearch still logs an "unknown mlockall error 0" on startup, the preceding setting isn't in effect


=== Elasticsearch ===

wget 'https://github.com/downloads/elasticsearch/elasticsearch/elasticsearch-0.19.4.deb'
sudo ln -s /usr/lib/jvm/java-6-openjdk-i386 /usr/lib/jvm/java-6-openjdk
sudo dpkg -i elasticsearch-0.19.4.deb
rm elasticsearch-0.19.4.deb

sudoedit /etc/default/elasticsearch
--
export ES_JAVA_OPTS=-server
export ES_HEAP_SIZE=1g
--

sudoedit /etc/elasticsearch/logging.yml
# replace console,file with syslog and add:
--
  syslog:
    type: syslog
    syslogHost: localhost
    facility: user
    facilityPrinting: true
    layout:
      type: pattern
      conversionPattern: "[%p][%c] %m%n"
--

sudoedit /etc/elasticsearch/elasticsearch.yml
--
bootstrap.mlockall: true
cluster.name: zeno
plugin.mandatory: cloud-aws
cloud:
    aws:
        access_key: AKIAI7MHM3G2FTHQJJ3A
        secret_key: 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
        region: us-east-1
discovery:
    type: ec2
--

TODO:
--
gateway:
    type: s3
    s3:
        bucket: bucket_name
--

sudo /usr/share/elasticsearch/bin/plugin -install elasticsearch/elasticsearch-cloud-aws/1.5.0
sudo /etc/init.d/elasticsearch restart


=== rsyslogd for Papertrail/Loggly/Splunk ===

sudoedit /etc/rsyslog.d/80-papertrail.conf
---
$ModLoad imudp
$UDPServerRun 514
#*.warn  @logs.papertrailapp.com:20382
#*.warn  @@logs.loggly.com:28388
*.warn  @@logs3.splunkstorm.com:20502
---

sudo service rsyslog restart

logger -p err just kidding


=== Sematext SPM ===

sudo aptitude install ntp
wget http://apps.sematext.com/spm-dist/spm-client-es-ubuntu-1.5.0.zip
unzip -q spm-client-es-ubuntu-*.zip
cd spm-client-es-ubuntu-*
nano nano spm-setup.properties
--
spm_sender_system_token=XXX
--
sudo bash spm-client-install.sh 2>&1 | tee -a /tmp/spm-install.log
sudoedit /etc/init.d/elasticsearch
--
ES_JAVA_OPTS="-server -Dcom.sun.management.jmxremote -javaagent:/spm/spm-monitor/lib/spm-monitor-es-1.5.0-withdeps.jar=/spm/spm-monitor/conf/spm-monitor-config-83dae3f9-1a35-458c-b6ca-e1e6eec43d1b-default.xml"
--
sudo service elasticsearch restart
cd ..
rm -rf spm-client-es-ubuntu-*


=== New Relic Agent ===

# download newrelic_agent2.6.0.zip
sudo unzip -q -d /usr/lib/newrelic newrelic_agent2.6.0.zip
rm newrelic_agent2.6.0.zip
sudoedit /usr/lib/newrelic/newrelic.yml
# set app_name=Play|Elasticsearch

sudoedit /etc/default/elasticsearch
--
ES_JAVA_OPTS="-server -javaagent:/usr/lib/newrelic/newrelic.jar -Dnewrelic.environment=elasticsearch"
--
sudo service elasticsearch restart

sudoedit /etc/init/play.conf
--
env JAVA_OPTS="... -javaagent:/usr/lib/newrelic/newrelic.jar -Dnewrelic.environment=play"
--
sudo service play restart


=== New Relic Server Monitoring ===

sudo wget -O /etc/apt/sources.list.d/newrelic.list http://download.newrelic.com/debian/newrelic.list
sudo apt-key adv --keyserver hkp://subkeys.pgp.net --recv-keys 548C16BF
sudo aptitude update
sudo aptitude install newrelic-sysmond
sudo nrsysmond-config --set license_key=93915b5044846b31eec878fee1dac53bf5cc3ce3
sudo /etc/init.d/newrelic-sysmond start


=== Scout ===

sudo aptitude install rubygems
sudo gem install scout
echo e4b65861-5f91-49f0-9e0b-b8014ee55ee4 | scout
crontab -e
--
* * * * *  /usr/local/bin/scout e4b65861-5f91-49f0-9e0b-b8014ee55ee4
--


=== Play ===

sudo adduser --system --no-create-home play

sudoedit /etc/init/play.conf
--
...
--

sudoedit /etc/play.conf
--
include "application.conf"

hostname="https://zenobase.com"

es.cluster=zeno

mail.smtp.auth=true
mail.smtp.starttls.enable=true
mail.smtp.host="smtp.sendgrid.com"
mail.smtp.port=587
mail.user="zenomail"
mail.pass="u140pseen2"
mail.from="info@zenobase.com"
--

wget https://s3.amazonaws.com/zeno-deploy/zenobase-1.0.xxxxx.zip

# if updating
sudo service play stop
sudo rm -r /var/play

unzip -q zenobase-*.zip && rm zenobase-*.zip
sudo mv zenobase-* /var/play
sudo chmod +x /var/play/start
sudo chown -R play:nogroup /var/play
sudo service play start

watch curl -s localhost:9000/status
