Developer Guide
===============

To refresh the dependencies in Eclipse, open the play console and run:

  eclipsify with-source=true


js libs
-------

bootstrap-mtt.js: concatenate modal + tabs + tooltips + typeahead
*.gz: gzip -9 *.min.js
