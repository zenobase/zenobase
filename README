Developer Guide
===============

To refresh the dependencies in Eclipse, open the play console and run:

  eclipse with-source=true


js libs
-------

bootstrap-2.2.x.js: concatenate modal + tabs + typeahead


Functional Tests
----------------

From the play console, run:
  
  test-only com.zenobase.testing.BrowserTest

To skip the functional tests, set:

  -Dwebdriver.enabled=false
